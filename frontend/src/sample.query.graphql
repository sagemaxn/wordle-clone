mutation Guess($guess: String!) {
    
  word(guess: $guess){
    word{
      color
      letter
    }
  }

}

query Answer{
  answer
}

query InDictionary($guess: String!){
  inDictionary(guess: $guess)
}
